# VABS Lasthands Replay Notes

The refinement keeps deterministic replay as the preferred evidence path. Replay payloads should include starting grid, cascade steps, final grid, win summary, cap state, feature state, bonus-buy state, RNG draw references, state version,
and round completion.

Screenshot binary storage remains unverified. Do not store screenshot binaries unless GS/product/compliance evidence requires them.

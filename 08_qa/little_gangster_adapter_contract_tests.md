# Little Gangster Adapter Contract Tests

Status: TEST_PLAN_ONLY_IMPLEMENTATION_BLOCKED

## Required Future Tests

- Adapter maps authoritative v0.3 result to `presentationPayload.gamePayload.payload`.
- Adapter includes generic presentation fields for shell/UI-kit compatibility.
- Adapter rejects or ignores browser-supplied authoritative result fields.
- Adapter keeps RNG/outcome/win/cap/feature truth backend-owned.
- Adapter keeps wallet/accounting fields outside `gamePayload`.
- Adapter can produce reconnect/history render payload snapshots.
- Adapter output validates against patched core schema.
- Static fixture strict-runtime variants match adapter output shape.

No adapter tests were created or run in this sprint because backend adapter implementation is not approved.
